# 明天开机说：继续干

## 触发词
说 **「继续干」** 我会自动加载今天（2026-06-29）的完整上下文继续处理。

## 或者直接说具体任务
- **「bot worker不动了」** — 检查 VPS
- **「数据看板又没了」** — 重新部署
- **「继续搞数据字段」** — 完善 following/posts_count
- **「继续做库存」** — 完善进销存功能

## 当前状态（2026-06-29）

### ✅ 今天全部修好的

**① Neon 数据库修复（最重要）**
- 旧 `/v2/query` 废弃 → 改用 `/sql` + `neon-connection-string` header
- 所有 `neon()` WebSocket 调用改为 HTTP ✅
- DataDashboard 数据稳定了 ✅

**② 库存系统完整上线**
- 入库：大箱(×100盒) / 小箱(×50盒) / 零散 / 消毒 ✅
- 出库：B端客户 / C端客户 / 样品(B和C) ✅
- 盘点：货架扫描 / 自动归总 / 按货架或按型号查看 / 一键调整 ✅
- 客户订单查询 ✅
- 入库汇总 / 出库汇总 ✅

**③ Bot 账号恢复**
- D1 持久化，不再依赖 localStorage ✅
- Add Account 按钮可用 ✅
- 编辑 / 删除 / 创建时间显示 ✅
- 修复同步漏洞（空数组不清表）✅

**④ Automation 页面**
- Account Fleet 能显示 D1 中的 bot ✅
- 账号卡片有创建日期、编辑、删除按钮 ✅

**⑤ 侧边栏**
- 导航可滚动，用户信息固定在底部不遮挡 ✅

### 部署方式
- CF Pages: `npx wrangler pages deploy dist --project-name=harvests --branch=main`
- CF Worker: `npx wrangler deploy --config wrangler.toml`（从 harvests-cloud-api 目录）

### 未完成
- bot 历史数据需 VPS 重新同步
- 货架标签还没贴
- 盘点数据没存数据库（目前只在内存）

## 重要路径
- 主仓库: `F:/inkflow app/InkFlow_Project/`
- 前端: `F:/inkflow app/InkFlow_Project/src/`
- Worker: `F:/inkflow app/InkFlow_Project/harvests-cloud-api/src/index.ts`
- API Worker URL: `https://harvests-cloud-api.inkflowapp.workers.dev`
- CF Pages: `harvests.pages.dev`（Production, main分支）
- VPS: 163.245.212.169 (RDP)
- CF 账号: owlink.liu@gmail.com (harvests)
