node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example

# build / temp artifacts
harvests-cloud-api/tmp/
inkflow_harvests/temp-stash-diff.txt

# cleanup: debug stashes, unrelated assets, leaked creds
.leaked-credentials-backup.md
stash_*.patch
stash_*.tsx
stash_*.txt
*_backup.json
_zip_review/
gen-lang-client-*.json

# LOCAL-ONLY secret backups (never commit — contain live credentials)
SECRETS_BACKUP.local.md
gemini-service-account.backup.json
.DS_Store

# bot worker browser profiles (cookies/localStorage/cache — never commit)
profiles/
