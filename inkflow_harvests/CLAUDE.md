# Boundaries
- Never read or decrypt browser password stores (Chrome Login Data, etc.)
- Only open browsers when explicitly asked
- Only search the project directory, not the user's system
- If the user says "I'll do it myself", stop and wait
- Ask before accessing any file outside this project
